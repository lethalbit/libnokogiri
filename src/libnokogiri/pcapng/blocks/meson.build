libnokogiri_headers_pcapng_blocks = files([
	'interface_description.hh',
	'section_header.hh',
])

if not meson.is_subproject()
	install_headers(
		libnokogiri_headers_pcapng_blocks,
		subdir: 'libnokogiri/pcapng/blocks'
	)
endif
